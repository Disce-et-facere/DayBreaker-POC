
when msg-Handler receives "hello" from topic msgHandler/android/ping ->
Send message "hello" to topic device/ping ->
wait for return message on topic msgHandler/ping/response with message "macaddress" ->
store the macaddress as a new device in the DB if it doesnt exist ->
return mac address of all devices in the DB to android/devices as one message per mac address.
if alias exist in the db send that to android/alias/macaddr ->
msg-Handler now need to dynamically subscribe to msgHandler/alias/macaddr and msgHandler/command/macaddr with diffrent mac addresses.
and do this for each message with a mac address in msgHandler/ping.

when msg-Handler receives message with a alias on topic msgHandler/macAddr/alias ->
Store/change message value as alias in DB for the device that has the same macAddress as the topic

when msg-Handler receieves msgHandler/command/macaddr "up" or "down"
store value as 100 for up and 0 for down for position in BD
and send "CW" for up or "CCW" for down to topic device/macaddr 


